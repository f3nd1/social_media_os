# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository (`social_media_os`) is currently empty — it has no source code, build configuration, or documentation yet. There are no build, lint, or test commands to run.

## Guidance

- When the first code is added, update this file with the project's actual setup: how to install dependencies, build, lint, run tests (including a single test), and a summary of the architecture.
- Do not assume a language, framework, or project structure until one is established in the repository.
